version: '3.8'

services:
  site:
    build: .
    #    command: ['npm', 'run', 'start:docker']
    ports:
      - '3000:3000'
    env_file:
      - .env.local
    volumes:
      - .:/home/node/app
    external_links:
      - 'host.docker.internal'
    depends_on:
      - api

  api:
    image: ghcr.io/samuel-ricardo/employee-dashboard_backend:main
    #restart: always
    ports:
      - '3001:3000'
    env_file:
      - .env
    depends_on:
      - mongo
    external_links:
      - 'host.docker.internal'

  mongo:
    image: mongo:latest
    #    restart: always
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    volumes:
      - ./.docker/data/db:/data/db
    external_links:
      - 'host.docker.internal'
